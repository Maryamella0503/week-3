#include<stdio.h>

int main(){
    float temp;
    printf("what is the current temperature in degrees?");
    scanf("%f", &temp);

    if (temp<=40){
        if (temp <-10);{
        printf("the temperatuure is between -10 and 40 degrees\n"); 
        }
    }
    else 
        printf("the temperature is not between -10 and 40 degrees");
    return 0;
}