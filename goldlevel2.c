#include<stdio.h>

int main(){
    int label;
    printf("enter a number:\n");
    scanf("%d", &label);
    switch (label) {
        if (label != -1){

            printf("invalid selection\n");
        }
    }

return 0;
}